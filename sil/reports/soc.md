# SOC — estimation, initialization and upstream signal chain — Test Report

> Generated by `sil/tests/report.py` from the executed tests. Do not edit by hand: objective and procedure come from the test source, results from the run.

The SOC feature itself: `src/battery/Bms_Soc.c` and the paths feeding it through `Battery_Monitor`. Covers the integration law and clamping, the pack blend, the three-tier startup chain, and upstream faults whose consequence lands on the SOC output.

Source: `sil/tests/test_soc.py`  ·  Index: [../TEST_REPORT.md](../TEST_REPORT.md)

## Execution environment

| | |
|---|---|
| Executed (UTC) | 2026-09-06 02:34:33 |
| Host | Windows 10 (AMD64) |
| Host compiler | gcc.exe (MinGW-W64 x86_64-ucrt-posix-seh, built by Brecht Sanders, r4) 16.1.0 |
| Python | 3.10.11 |
| pytest | 9.1.1 |
| SIL library | bms_sil.dll, 124821 bytes, built 2026-09-05 22:59:27 UTC |
| Repo commit | 4cac175 |

## Summary

**21 test cases** (24 executions) in 0.59 s.

| Outcome | Count |
|---|---|
| PASS | 19 |
| XFAIL (known defect) | 1 |
| SKIPPED | 1 |

**Feature verdict: PASS**

## Cases

| Case | Test | Requirement | Result |
|---|---|---|---|
| SA-03 | [test_SA_03_blend_reference_degenerate](#test-sa-03-blend-reference-degenerate) | SOC-FR-03, SOC-FR-04 | PASS |
| — | [test_SA_blend_formula_reference](#test-sa-blend-formula-reference) | SOC-FR-03, SOC-FR-04 | PASS |
| — | [test_blend_divergence_is_unreachable](#test-blend-divergence-is-unreachable) | SOC-FR-02 | PASS |
| SA-05 | [test_SA_05_clamps_at_empty](#test-sa-05-clamps-at-empty) | SOC-FR-11 | PASS |
| SA-06 | [test_SA_06_clamps_at_full](#test-sa-06-clamps-at-full) | SOC-FR-11 | PASS |
| SA-07 | [test_SA_07_set_soc_clamps_above_full](#test-sa-07-set-soc-clamps-above-full) | SOC-FR-11 | PASS |
| SA-08 | [test_SA_08_sign_convention](#test-sa-08-sign-convention) | SOC-FR-01, SOC-IR-01 | PASS |
| SA-09 | [test_SA_09_one_c_discharge_scale](#test-sa-09-one-c-discharge-scale) | SOC-FR-01, SOC-TR-01 | PASS |
| — | [test_coulomb_integration_matches_hand_calculation](#test-coulomb-integration-matches-hand-calculation) | SOC-FR-01 | PASS |
| IT-02 | [test_IT_02_erased_flash_gives_default_and_invalid](#test-it-02-erased-flash-gives-default-and-invalid) | SOC-FR-06, SOC-FR-07 | PASS |
| IT-01 | [test_IT_01_restores_from_nvm](#test-it-01-restores-from-nvm) | SOC-FR-06, SOC-FR-07 | PASS |
| IT-03 | [test_IT_03_stale_soc1_records_fall_back_to_default](#test-it-03-stale-soc1-records-fall-back-to-default) | SOC-FR-06, SOC-FR-07 | PASS |
| IT-05 | [test_IT_05_invalid_start_becomes_valid_once_integrating](#test-it-05-invalid-start-becomes-valid-once-integrating) | SOC-FR-06, SOC-FR-07 | PASS |
| IT-06 | [test_IT_06_invalid_start_is_never_persisted](#test-it-06-invalid-start-is-never-persisted) | SOC-FR-06, SOC-FR-07 | PASS |
| IT-04 | [test_IT_04_ocv_reset_tier](#test-it-04-ocv-reset-tier) | SOC-FR-06, SOC-FR-07 | SKIPPED |
| IT-07 | [test_IT_07_init_source_is_latched_not_recomputed](#test-it-07-init-source-is-latched-not-recomputed) | SOC-FR-06, SOC-FR-07 | PASS |
| IT-08 | [test_IT_08_init_source_fits_the_can_signal_width](#test-it-08-init-source-fits-the-can-signal-width) | SOC-FR-06, SOC-FR-07 | PASS |
| CH-01 | [test_CH_01_current_dropout_invalidates_but_holds_soc](#test-ch-01-current-dropout-invalidates-but-holds-soc) | SOC-FR-12 | PASS |
| CH-03 | [test_CH_03_vafe_average_matches_mean_of_cells](#test-ch-03-vafe-average-matches-mean-of-cells) | SOC-IR-02 | PASS |
| — | [test_CH_single_alive_glitch_recovers](#test-ch-single-alive-glitch-recovers) | SOC-FR-12, SOC-IR-01 | PASS |
| — | [test_CH_stuck_alive_counter_must_invalidate_current](#test-ch-stuck-alive-counter-must-invalidate-current) | SOC-FR-12, SOC-IR-01 | XFAIL (known defect) |

## Case detail

### test_SA_03_blend_reference_degenerate

| | |
|---|---|
| Case ID | SA-03 |
| Requirement | SOC-FR-03, SOC-FR-04 |
| Result | PASS |
| Duration | 0.6 ms |
| Source | `sil/tests/test_soc.py:42` |

**Objective.** SA-03 (partial): the blend reduces to the common value when all three agree.

> In the current build Min == Max == Avg always (see SOC_DESIGN.md 5.1), so
> only the degenerate case is reachable end-to-end. The non-degenerate blend
> is covered by test_SA_blend_formula_reference below, against the same
> formula the C implements.

**Procedure.**

```python
for target in (0, 123, 500, 999, MAX_PCT_X10):
    bms.set_soc(target)
    assert bms.soc_min == target
    assert bms.soc_max == target
    assert bms.soc_avg == target
    assert bms.soc == target
```

### test_SA_blend_formula_reference

| | |
|---|---|
| Case ID | — |
| Requirement | SOC-FR-03, SOC-FR-04 |
| Result | PASS |
| Duration | 1.6 ms over 4 variants |
| Source | `sil/tests/test_soc.py:58` |

**Objective.** SA-01/02/03/04: the documented blend formula, checked as arithmetic.

> NOTE: this exercises the Python mirror in bms_sil.expected_blend(), not the
> C. The C path cannot be driven to distinct Min/Max/Avg in this build; see
> test_blend_divergence_is_unreachable.

**Procedure.**

```python
assert expected_blend(soc_min, soc_max, soc_avg) == expected
```

### test_blend_divergence_is_unreachable

| | |
|---|---|
| Case ID | — |
| Requirement | SOC-FR-02 |
| Result | PASS |
| Duration | 1.9 ms |
| Source | `sil/tests/test_soc.py:77` |

**Objective.** Documents SOC_DESIGN.md 5.1 empirically rather than by assertion of prose.

> Nothing in the reachable API can make the three estimators differ: they
> integrate the same current against the same capacity, and the only seeding
> path that could separate them (the OCV reset) is unreachable. If this test
> ever fails, divergence has become possible and the blend needs real
> end-to-end coverage.

**Procedure.**

```python
bms.set_soc(500)
bms.run_normal(5000, current_mA=-40_000, cell_mV=NOMINAL_CELL_MV)
assert bms.soc_min == bms.soc_max == bms.soc_avg
bms.run_normal(5000, current_mA=+40_000, cell_mV=NOMINAL_CELL_MV)
assert bms.soc_min == bms.soc_max == bms.soc_avg
```

### test_SA_05_clamps_at_empty

| | |
|---|---|
| Case ID | SA-05 |
| Requirement | SOC-FR-11 |
| Result | PASS |
| Duration | 9.4 ms |
| Source | `sil/tests/test_soc.py:93` |

**Objective.** SA-05: sustained discharge saturates at 0 % and does not wrap.

**Procedure.**

```python
bms.set_soc(20)  # 2.0 % = 2000 mAh; at 100 A that is 72 s of discharge
bms.run_normal(120_000, current_mA=-100_000, cell_mV=3000)
assert bms.soc == 0
# Keep discharging: must stay at 0, not wrap to a large value.
bms.run_normal(60_000, current_mA=-100_000, cell_mV=3000)
assert bms.soc == 0
assert bms.capacity_avg_mAh == pytest.approx(0.0, abs=1e-3)
```

### test_SA_06_clamps_at_full

| | |
|---|---|
| Case ID | SA-06 |
| Requirement | SOC-FR-11 |
| Result | PASS |
| Duration | 13.2 ms |
| Source | `sil/tests/test_soc.py:104` |

**Objective.** SA-06: sustained charge saturates at 100 %.

**Procedure.**

```python
bms.set_soc(980)
bms.run_normal(120_000, current_mA=+100_000, cell_mV=3900)
assert bms.soc == MAX_PCT_X10
bms.run_normal(60_000, current_mA=+100_000, cell_mV=3900)
assert bms.soc == MAX_PCT_X10
assert bms.capacity_avg_mAh == pytest.approx(float(PACK_CAPACITY_MAH), rel=1e-6)
```

### test_SA_07_set_soc_clamps_above_full

| | |
|---|---|
| Case ID | SA-07 |
| Requirement | SOC-FR-11 |
| Result | PASS |
| Duration | 0.6 ms |
| Source | `sil/tests/test_soc.py:114` |

**Objective.** SA-07: Bms_Soc_SetSoc_pct_x10 clamps an out-of-range request to 100 %.

**Procedure.**

```python
bms.set_soc(1500)
assert bms.soc == MAX_PCT_X10
assert bms.soc_avg == MAX_PCT_X10
```

### test_SA_08_sign_convention

| | |
|---|---|
| Case ID | SA-08 |
| Requirement | SOC-FR-01, SOC-IR-01 |
| Result | PASS |
| Duration | 76.9 ms |
| Source | `sil/tests/test_soc.py:121` |

**Objective.** SA-08: positive current charges, negative discharges.

> This is the convention the over-current thresholds in Battery_Monitor.c
> imply (CHARGE_OC = +80000, DISCHARGE_OC = -100000).

**Procedure.**

```python
bms.set_soc(500)
bms.run_normal(600_000, current_mA=+50_000, cell_mV=NOMINAL_CELL_MV)
charged = bms.soc
assert charged > 500, "positive current must raise SOC"

bms.set_soc(500)
bms.run_normal(600_000, current_mA=-50_000, cell_mV=NOMINAL_CELL_MV)
assert bms.soc < 500, "negative current must lower SOC"
```

### test_SA_09_one_c_discharge_scale

| | |
|---|---|
| Case ID | SA-09 |
| Requirement | SOC-FR-01, SOC-TR-01 |
| Result | PASS |
| Duration | 320.7 ms |
| Source | `sil/tests/test_soc.py:137` |

**Objective.** SA-09: a 1 C discharge for one hour takes a full pack from 100 % to ~0 %.

> Validates the dt_h scale factor end-to-end, not just its sign.

**Procedure.**

```python
bms.set_soc(MAX_PCT_X10)
# 1 C for this pack is 100 A. Run one hour of virtual time.
bms.run_normal(3_600_000, current_mA=-PACK_CAPACITY_MAH, cell_mV=NOMINAL_CELL_MV)
assert bms.soc == pytest.approx(0, abs=5)   # within 0.5 %
```

### test_coulomb_integration_matches_hand_calculation

| | |
|---|---|
| Case ID | — |
| Requirement | SOC-FR-01 |
| Result | PASS |
| Duration | 69.7 ms |
| Source | `sil/tests/test_soc.py:148` |

**Objective.** The integrated charge matches mAh computed independently in Python.

**Procedure.**

```python
bms.set_soc(500)
start = bms.capacity_avg_mAh

current_mA = -30_000
duration_ms = 600_000          # 10 minutes
bms.run_normal(duration_ms, current_mA=current_mA, cell_mV=NOMINAL_CELL_MV)

expected = start + charge_mAh(current_mA, duration_ms)
assert bms.capacity_avg_mAh == pytest.approx(expected, rel=1e-3)
```

### test_IT_02_erased_flash_gives_default_and_invalid

| | |
|---|---|
| Case ID | IT-02 |
| Requirement | SOC-FR-06, SOC-FR-07 |
| Result | PASS |
| Duration | 0.4 ms |
| Source | `sil/tests/test_soc.py:166` |

**Objective.** IT-02: no valid record -> tier 3, default SOC, estimates flagged invalid.

**Procedure.**

```python
assert bms.soc == INITIAL_PCT_X10
assert bms.soc_min == INITIAL_PCT_X10
assert bms.soc_max == INITIAL_PCT_X10
assert bms.soc_avg == INITIAL_PCT_X10
assert bms.soc_valid is False
assert bms.soc_legacy_valid is False
assert bms.init_source == INIT_SOURCE_DEFAULT
```

### test_IT_01_restores_from_nvm

| | |
|---|---|
| Case ID | IT-01 |
| Requirement | SOC-FR-06, SOC-FR-07 |
| Result | PASS |
| Duration | 5.6 ms |
| Source | `sil/tests/test_soc.py:177` |

**Objective.** IT-01: a persisted record is restored into all three estimators, valid.

**Procedure.**

```python
bms.set_soc(732)
bms.run_normal(61_000, current_mA=0, cell_mV=NOMINAL_CELL_MV)
assert bms.nvm_records >= 1, "expected a persisted record before power cycle"

saved = bms.nvm_load()
assert saved is not None

bms.power_cycle()

assert bms.soc_min == saved[0]
assert bms.soc_max == saved[1]
assert bms.soc_avg == saved[2]
assert bms.soc_valid is True, "a real NVM reference must come up valid"
assert bms.init_source == INIT_SOURCE_NVM
```

### test_IT_03_stale_soc1_records_fall_back_to_default

| | |
|---|---|
| Case ID | IT-03 |
| Requirement | SOC-FR-06, SOC-FR-07 |
| Result | PASS |
| Duration | 0.4 ms |
| Source | `sil/tests/test_soc.py:195` |

**Objective.** IT-03: old 16-byte "SOC1" records are rejected -> one boot at tier 3.

**Procedure.**

```python
bms = bms_dirty

# Lay down plausible-looking legacy records: magic "SOC1", 16-byte stride.
legacy_magic = (0x31, 0x43, 0x4F, 0x53)     # 0x534F4331, little endian
for slot in range(4):
    base = slot * 16
    for i, byte in enumerate(legacy_magic):
        bms.flash_write_byte(base + i, byte)
    for i in range(4, 16):
        bms.flash_write_byte(base + i, 0x00)

bms.power_on()

assert bms.soc == INITIAL_PCT_X10
assert bms.soc_valid is False, "stale-format records must not be trusted"
assert bms.init_source == INIT_SOURCE_DEFAULT
```

### test_IT_05_invalid_start_becomes_valid_once_integrating

| | |
|---|---|
| Case ID | IT-05 |
| Requirement | SOC-FR-06, SOC-FR-07 |
| Result | PASS |
| Duration | 0.7 ms |
| Source | `sil/tests/test_soc.py:215` |

**Objective.** IT-05: a tier-3 blind start flips to valid on the first good current tick.

> Documented behaviour, not an accident - see SOC_DESIGN.md 5.7.

**Procedure.**

```python
assert bms.soc_valid is False
bms.run_normal(1000, current_mA=-10_000, cell_mV=NOMINAL_CELL_MV)
assert bms.soc_valid is True
```

### test_IT_06_invalid_start_is_never_persisted

| | |
|---|---|
| Case ID | IT-06 |
| Requirement | SOC-FR-06, SOC-FR-07 |
| Result | PASS |
| Duration | 1.5 ms |
| Source | `sil/tests/test_soc.py:225` |

**Objective.** IT-06: while the estimate is a blind guess, nothing is written to flash.

**Procedure.**

```python
assert bms.soc_valid is False
# Run well past the save period, but never supply a valid current.
bms.run_ms(120_000)
assert bms.flash_writes == 0
assert bms.nvm_records == 0
```

### test_IT_04_ocv_reset_tier

| | |
|---|---|
| Case ID | IT-04 |
| Requirement | SOC-FR-06, SOC-FR-07 |
| Result | SKIPPED |
| Duration | 0.5 ms |
| Source | `sil/tests/test_soc.py:234` |

**Objective.** IT-04: OCV reset path. Blocked on a real sleep-time source.

**Procedure.**

```python
"""IT-04: OCV reset path. Blocked on a real sleep-time source."""
```

**Reason.**

```
Skipped: Tier 1 (OCV reset) is unreachable: Bms_Soc_GetElapsedSleepTime_s() is hardcoded to 0 and CellVoltageValid is FALSE at init. See SOC_DESIGN.md 5.2.
```

### test_IT_07_init_source_is_latched_not_recomputed

| | |
|---|---|
| Case ID | IT-07 |
| Requirement | SOC-FR-06, SOC-FR-07 |
| Result | PASS |
| Duration | 57.0 ms |
| Source | `sil/tests/test_soc.py:243` |

**Objective.** IT-07: the init source reports how SOC was *seeded*, not its current state.

> It is latched by Bms_Soc_InitPack() and must not be rewritten by runtime
> integration - a consumer reading CAN 0x308 Pack1SOCInitSource needs the
> provenance of the anchor, which does not change as the counter moves.

**Procedure.**

```python
bms.set_soc(700)
bms.run_normal(61_000, current_mA=0, cell_mV=NOMINAL_CELL_MV)
bms.power_cycle()

assert bms.init_source == INIT_SOURCE_NVM
source_at_boot = bms.init_source

# Integrate for a while; the SOC moves, the provenance must not.
bms.run_normal(600_000, current_mA=-30_000, cell_mV=NOMINAL_CELL_MV)

assert bms.soc != 700, "precondition: SOC should have moved"
assert bms.init_source == source_at_boot
```

### test_IT_08_init_source_fits_the_can_signal_width

| | |
|---|---|
| Case ID | IT-08 |
| Requirement | SOC-FR-06, SOC-FR-07 |
| Result | PASS |
| Duration | 0.3 ms |
| Source | `sil/tests/test_soc.py:264` |

**Objective.** IT-08: every init-source value fits the 3-bit CAN field on 0x308.

> Pack1SOCInitSource is 17|3@1+, so any value above 7 would be silently
> truncated by Bms_Can_SendSocStatus().

**Procedure.**

```python
assert 0 <= bms.init_source <= 7
```

### test_CH_01_current_dropout_invalidates_but_holds_soc

| | |
|---|---|
| Case ID | CH-01 |
| Requirement | SOC-FR-12 |
| Result | PASS |
| Duration | 0.5 ms |
| Source | `sil/tests/test_soc.py:278` |

**Objective.** CH-01: losing pack current invalidates the estimate but holds the value.

> Exercises the vPACK timeout upstream of SOC: frames simply stop arriving.

**Procedure.**

```python
bms.set_soc(640)
bms.run_normal(2000, current_mA=-10_000, cell_mV=NOMINAL_CELL_MV)
assert bms.soc_valid is True
held = bms.soc

# Stop injecting current frames; vPACK times out after 10 x 100 ms.
bms.run_ms(2000)

assert bms.pack_current_valid is False
assert bms.soc_valid is False
assert bms.soc == held, "SOC value must be held, not zeroed"
```

### test_CH_03_vafe_average_matches_mean_of_cells

| | |
|---|---|
| Case ID | CH-03 |
| Requirement | SOC-IR-02 |
| Result | PASS |
| Duration | 0.5 ms |
| Source | `sil/tests/test_soc.py:296` |

**Objective.** CH-03: the vAFE average equals the mean of the 16 injected cells.

> Verifies the statistic added to Bms_Vafe_UpdateStatistics, propagated
> through Battery_Monitor's unit conversion.

**Procedure.**

```python
cells = [3500 + (i * 10) for i in range(16)]     # 3500..3650 mV
bms.run_ms(100, cells_mV=cells, current_mA=0, pack_mV=57_600)

assert bms.cell_valid is True
assert bms.cell_min_V == pytest.approx(min(cells) / 1000.0, abs=1e-6)
assert bms.cell_max_V == pytest.approx(max(cells) / 1000.0, abs=1e-6)

expected_avg_mV = sum(cells) // 16      # integer mean, as the C computes it
assert bms.cell_avg_V == pytest.approx(expected_avg_mV / 1000.0, abs=1e-6)
```

### test_CH_single_alive_glitch_recovers

| | |
|---|---|
| Case ID | — |
| Requirement | SOC-FR-12, SOC-IR-01 |
| Result | PASS |
| Duration | 0.4 ms |
| Source | `sil/tests/test_soc.py:313` |

**Objective.** A one-off alive-counter glitch costs one cycle of data and then resyncs.

> Bms_Vpack re-bases the expected counter from whatever it last received, so
> an isolated jump invalidates exactly one cycle. That is intended behaviour.

**Procedure.**

```python
bms.run_normal(2000, current_mA=-5_000, cell_mV=NOMINAL_CELL_MV)
assert bms.soc_valid is True

bms.skip_alive_once()
bms.run_normal(500, current_mA=-5_000, cell_mV=NOMINAL_CELL_MV)

assert bms.pack_current_valid is True, "chain should resync after one glitch"
assert bms.soc_valid is True
```

### test_CH_stuck_alive_counter_must_invalidate_current

| | |
|---|---|
| Case ID | — |
| Requirement | SOC-FR-12, SOC-IR-01 |
| Result | XFAIL (known defect) |
| Duration | 32.0 ms |
| Source | `sil/tests/test_soc.py:329` |

**Objective.** A *sustained* alive-counter failure must invalidate pack current.

> Scenario: the pack is discharging at 100 A when the vPACK transmitter
> sticks. Frames keep arriving and now report 0 A, but the alive counter
> never advances. The stale -100 A must not keep driving the integrator, and
> the SOC must not be advertised as valid.

**Procedure.**

```python
bms.run_normal(2000, current_mA=-100_000, cell_mV=NOMINAL_CELL_MV)
assert bms.soc_valid is True
soc_before = bms.soc

bms.freeze_alive(True)
bms.run_normal(600_000, current_mA=0, cell_mV=NOMINAL_CELL_MV)   # 10 min at rest

assert bms.pack_current_valid is False, (
    f"stuck alive counter must invalidate pack current, but current reads "
    f"{bms.pack_current_mA} mA and is still flagged valid"
)
assert bms.soc_valid is False
assert bms.soc == soc_before, "SOC must not drift on a frozen current value"
```

**Reason.**

```
DEFECT: Battery_Monitor.c:286 sets PackCurrentValid[0] from g_BmsVpackData.CurrentValid (frame arrival only), while the data copy is gated on g_BmsVpackData.Valid (which includes AliveValid). A stuck alive counter therefore freezes PackCurrent_mA at its last value while still reporting it valid, and SOC integrates the stale current forever.
```

<!-- sil-report slug='soc' title='SOC — estimation, initialization and upstream signal chain' cases='21' duration='0.594' verdict='PASS' executed='2026-09-06 02:34:33' passed='19' xfailed='1' skipped='1' xpassed='0' failed='0' error='0' -->
